using System;
using System.Collections.Generic;
using System.Text;

namespace Identory
{
    public class Settings : IdentoryEndpoint
    {
        public Settings(string endpoint) : base(endpoint)
        {
        }
    }
}
